[package]
name = "nimiq-nano-blockchain"
version = "0.1.0"
authors = ["The Nimiq Core Development Team <info@nimiq.com>"]
description = "Block storage for Nimiq's Nano Nodes"
license = "Apache-2.0"
homepage = "https://nimiq.com"
repository = "https://github.com/nimiq/core-rs-albatross"
edition = "2018"

[dependencies]
nimiq-block-albatross = { path = "../primitives/block-albatross", version = "0.1" }
nimiq-genesis = { path = "../genesis", version = "0.1" }
nimiq-hash = { path = "../hash", version = "0.1" }
nimiq-primitives = { path = "../primitives", version = "0.1" }
nimiq-nano-sync = { path = "../nano-sync", version = "0.1", default-features = false, features = ["verifier"]  }