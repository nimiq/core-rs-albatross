/// This deals with everything necessary to aggregate either prevote or precommit aggregations.
mod aggregate;

/// This deals with everything a node does while waiting for a proposal.
mod await_proposal;

/// This deals with proposing a proposal.
mod propose;
